# Core (required for scripts 01-09 and verification)
numpy>=1.26,<2.0
pandas>=2.2,<3.0
scipy>=1.13,<2.0
statsmodels>=0.14,<1.0
scikit-learn>=1.4,<2.0
matplotlib>=3.8,<4.0
yfinance>=0.2.40,<0.3

# iNAV PDF parsing (script 02)
pypdf>=4.0,<5.0

# OPTIONAL -- only for the MSGARCH 2.5 backend in script 06.
# Requires R 4.x with `install.packages('MSGARCH')` already installed system-wide.
# Uncomment if you have R + MSGARCH:
# rpy2>=3.5,<4.0
